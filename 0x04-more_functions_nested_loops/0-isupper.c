#include "main.h"
/**
 * _isupper - check if the a letter is upper
 * @X: The numver to be checked 
 * Return: 1 fot upper letter or 0 for any else 
 */ 

int _isupper(int x)
{
	if (x>=65 && x <=90)
		return(1);
		return(0);

}
